const nacionalidad = "brasileño";

console.log(
  nacionalidad === "argentino"
    ? "tu documento empieza en 32"
    : "Tu documento empieza en 92"
);

//* Ternario es para cosas que son VERDAD o MENTIRA

//* Ya sabe si es true o false ? entonces no necesitamos la comparacion

const helado = false;

console.log(helado ? "ssiii" : "booooo");

const login = true;
console.log(login ? "redirigir a cart" : "redirigir a login");
